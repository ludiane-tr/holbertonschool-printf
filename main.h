#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <limits.h>
#include <stddef.h>
#include <stdarg.h>

/* Prototype */

int _putchar(char c);
int _printf(const char *format, ...);
int _char (va_list args);
int _percent(va_list args);

#endif
